{
  "name": "fissh",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node main.ts",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:fix": "oxfmt",
    "format:check": "oxfmt --check",
    "build:docker": "docker build .",
    "test": "vitest run"
  },
  "dependencies": {
    "ssh2": "^1.17.0"
  },
  "devDependencies": {
    "@types/node": "^25.7.0",
    "@types/ssh2": "^1.15.5",
    "oxfmt": "^0.60.0",
    "oxlint": "^1.66.0",
    "typescript": "^7.0.0",
    "vitest": "^4.0.0"
  }
}
